\begin{outline}
    \item starting point of the thesis
    \item ATL is a model transformation tooling
    \item ATLc is a plugin, which allows you to gather constraint expressions from ATL code and pass it to different solvers
    \item provides a object oriented constraint metamodel in ecore
    \item instantiates constraint models for LP and CP solvers
\end{outline}